<a href="<%= author_path %><%= h(id) %>/">
  <img src="<%= author_path %><%= h(slide.thumbnail_path) %>"
       alt="<%= h(slide.title) %>"
       title="<%= h(slide.title) %>"
       class="thumbnail">
</a>
